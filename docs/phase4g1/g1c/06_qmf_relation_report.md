# QMF relation 报告

weight 固定为 normalized discount × committed mass，不因结果改写。`LANGUAGE_QMF_RELATION = INCONCLUSIVE`；`FORENSIC_QMF_RELATION = PASS`。完整 Pearson/Spearman 与 CI 见 `outputs/phase4g1/g1c/qmf_metrics.json`。
